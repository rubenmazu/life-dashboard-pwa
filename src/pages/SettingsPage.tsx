import { Settings } from '@/components/settings/Settings';

/**
 * SettingsPage - Renders the Settings component.
 * Sections: Theme, Notifications, Data, Account, About.
 */
export default function SettingsPage() {
  return <Settings />;
}
